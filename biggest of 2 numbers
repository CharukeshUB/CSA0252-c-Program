#include<stdio.h>
#include<conio.h>
void main()
{
  int a,b;
  printf("Enter the A and B values:");
  scanf("%d %d",&a,&b);
  if(a>b)
    printf("%d is the biggest value",a);
  else if(b>a)
    printf("%d is the biggest value",b)
  else
    printf("%d is equal",a);
    getch();
}  
